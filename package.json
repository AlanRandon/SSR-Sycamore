{
  "scripts": {
    "build": "wasm-pack build --target web --out-dir ../dist/wasm ./client && npm run build:minify",
    "build:minify": "esbuild --minify --keep-names init.ts --outfile=dist/wasm/init.min.js",
    "start": "npm run build && cargo run"
  },
  "devDependencies": {
    "esbuild": "^0.15.16",
    "wasm-pack": "^0.10.3"
  }
}
