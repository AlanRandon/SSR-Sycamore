{
  "scripts": {
    "build": "wasm-pack build --target web --out-dir ../dist/wasm ./client && npm run build:ts",
    "build:ts": "esbuild --bundle=true --minify --format=esm init.ts --outfile=dist/wasm/init.min.js",
    "start": "npm run build && cargo run"
  },
  "devDependencies": {
    "esbuild": "^0.15.16",
    "wasm-pack": "^0.10.3"
  }
}
